{
  "name": "@opentrade-registry/record-api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Private v2 record search and verification API service for OpenTrade Registry.",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@opentrade-registry/core": "workspace:*",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/pg": "^8.15.6"
  }
}
